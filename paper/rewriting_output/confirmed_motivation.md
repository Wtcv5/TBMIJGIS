| Field | Content |
|---|---|
| Source | user-provided final repositioning note, received 2026-06-24; supersedes the earlier response-supervised GNN-GRU positioning |
| Confirmed motivation statement | Existing monitoring-based TBM response models preserve temporal dependence, but they do not explicitly represent the spatial relations between geological units and TBM components. The paper should therefore be positioned as a chainage-referenced rock--TBM spatial interaction graph model: TSP-derived rock voxels and parameterized TBM surface components are formalised as heterogeneous spatial entities, candidate rock--machine relations are constrained by active-zone membership, distance decay, surface-normal compatibility, and excavation state, and component--chainage spatial interaction descriptors are derived by aggregating TSP-derived geological anomaly scores over geometry-weighted candidate relations. The descriptors are evaluated through their consistency with persistence residuals of TBM monitoring responses, not through claims of broad forecasting superiority. |
| Red thread | Move from monitoring-only temporal response modelling to explicit spatial entity-relation modelling: the graph is necessary because rock voxels, TBM surface components, and geometry-compatible candidate relations naturally form a dynamic heterogeneous spatial graph. The paper's main value is component--chainage diagnostic representation and response-residual consistency, not GNN-GRU prediction. |
| Prioritized claims | 1. Rock--TBM interaction is a heterogeneous spatial entity-relation problem, not only a monitoring time-series problem. 2. Geometry-constrained candidate relations provide an auditable way to express plausible rock--machine proximity under active-zone, distance, normal-compatibility, and excavation-state constraints. 3. A simple TSP-derived geological anomaly score, preferably based on negative standardized $V_p$, can be aggregated over geometry-weighted candidate relations to produce component--chainage spatial interaction descriptors. 4. Persistence residuals provide a defensible evaluation target: temporal inertia is handled by persistence, while the spatial graph is tested for diagnostic consistency with the remaining response variation. 5. GNN-GRU should be reported only as an exploratory response-supervised graph readout, because current sample sizes and results do not support broad forecasting improvement. |
| Claims to avoid | 1. GNN-GRU as the main model. 2. Prediction improvement or SOTA forecasting as the central contribution. 3. Learned contact relevance, contact force, contact pressure, jamming probability, or calibrated risk estimation. 4. Causal attribution from descriptors or attention. 5. Treating component--chainage heatmaps as measured contact maps. |
| Section consequences | The title, abstract, Introduction, Methods, Results, Discussion, and Figure 1 must foreground chainage-referenced spatial interaction graphs, explicit component--chainage descriptors $A_c(t)$ and $I_c(t)$, and response-residual consistency. Methods should promote the former geometric prior $\pi_{ij}^{rm}$ to an explicit edge weight $w_{ij}^{rm}(t)$ and define a restrained TSP anomaly score $q_i$. Results should be reorganised around graph construction, component--chainage descriptor maps, residual consistency, spatial organisation checks, and only then an exploratory graph-learning readout. Discussion must explain why graph representation remains necessary even when GNN-GRU does not establish broad forecasting gains. |

## Confirmation Note

Motivation updated by user on 2026-06-24. The key adjustment is stronger than
the earlier GIScience framing: the main manuscript should no longer centre on a
response-supervised GNN-GRU. The defensible core is the explicit spatial graph
representation and component--chainage diagnostic descriptors, with GNN-GRU only
as a supplementary or final exploratory check.
