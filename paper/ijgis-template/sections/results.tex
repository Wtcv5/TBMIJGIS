\subsection{Constructed rock--machine interaction graph sequences}
\label{sec:constructed_graphs}
The first result is the constructed spatial relation object itself. For each
case, TSP-derived rock voxels and parameterized TBM surface nodes are mapped to
the same chainage-referenced coordinate system, and the candidate relation set
$E_t^{rm}$ is rebuilt at each excavation step. Figure~\ref{fig:geometry_edges}
illustrates the construction rule: candidate relations are screened by
active-zone membership, distance threshold, surface-normal compatibility, and
excavation state.

Table~\ref{tab:graph_construction_stats} summarises the resulting graph
objects in the test intervals. The BSLL runs use the same tunnel section with
different diagnostic horizons and therefore have the same graph size per step.
SJLS uses a coarser parameterized TBM surface but a comparable active-zone rock
voxel field. The candidate-edge counts show that the descriptors are computed
from explicit rock--machine relation sets rather than from conceptual links.

\begin{table}[htbp]
\centering
\caption{Graph construction statistics in the test intervals. Edge shares are
computed from candidate rock--machine relations in the main graph setting.}
\label{tab:graph_construction_stats}
\scriptsize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lrrrrr}
\toprule
Case & Rock nodes & TBM nodes & Candidate edges & Cutterhead share & Shield share \\
\midrule
BSLL h=1 & 3558 & 1352 & 14620 & 0.145 & 0.855 \\
BSLL h=3 & 3558 & 1352 & 14620 & 0.145 & 0.855 \\
SJLS h=3 & 3558 & 352 & 3834 & 0.159 & 0.841 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Component--chainage descriptor patterns and residual traces}
\label{sec:component_chainage_patterns}
The main graph-derived output is the component--chainage descriptor map built
from $I_c(t)$. In each case, cutterhead, front-shield, middle-shield, and
tail-shield values are computed across the test chainage interval as
geometry-weighted TSP anomaly summaries over candidate rock--machine relations.

Figure~\ref{fig:descriptor_evidence} links these descriptor maps to the fixed
primary residual traces. The heatmaps show where low-velocity anomaly exposure
is concentrated by component and chainage, while the lower panels show the
corresponding response residual selected for diagnostic checking. This layout
makes the evidence chain explicit: the graph first produces a spatial
component--chainage descriptor, and the residual trace then tests whether that
descriptor co-varies with response changes left after persistence.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.95\linewidth]{fig6_descriptor_evidence.pdf}
\caption{Component--chainage spatial interaction descriptor evidence and
primary residual traces. Upper panels show $I_c(t)$ values for BSLL h=1,
BSLL h=3, and SJLS h=3 using a common colour scale. Lower panels show the
fixed residual series used for the primary diagnostic pair in each case.}
\label{fig:descriptor_evidence}
\end{figure}

\subsection{Primary descriptor--residual associations}
\label{sec:residual_results}
The formal diagnostic statistic is Spearman correlation between $I_c(t)$ and
the persistence residual $e_{t+h}^{(k)}$. Table~\ref{tab:primary_diagnostic}
reports the fixed primary pairs together with raw Spearman correlation,
linearly detrended Spearman correlation, and the circular-shift diagnostic. The
values are descriptive and are not treated as confirmatory hypothesis tests.

\begin{table}[htbp]
\centering
\caption{Primary descriptor--residual diagnostics for fixed component--response
pairs. Circular-shift values are descriptive small-sample checks.}
\label{tab:primary_diagnostic}
\scriptsize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{llrrrr}
\toprule
Case & Primary pair & $n$ & Raw $\rho$ & Detrended $\rho$ & Shift check \\
\midrule
BSLL h=1 & Front shield--advance rate & 8 & 0.381 & 0.214 & 0.375 \\
BSLL h=3 & Front shield--shield pressure & 7 & -0.750 & -0.464 & 0.429 \\
SJLS h=3 & Cutterhead--shield pressure & 17 & -0.912 & -0.772 & 0.059 \\
\bottomrule
\end{tabular}
\end{table}

The SJLS case shows the most evident descriptor--residual co-variation. Its
cutterhead $I_c(t)$--shield-pressure residual association remains strong after
linear detrending ($\rho=-0.772$), whereas the BSLL evidence weakens under
detrending. This difference is consistent with the longer SJLS test interval
and clearer external TSP velocity contrast. The BSLL results are retained as
compact diagnostic checks, but they do not support a strong case-general rule.

For completeness, Table~\ref{tab:descriptor_residual} reports the full
component--response Spearman matrix for $I_c(t)$. The matrix is exploratory and
unadjusted for multiple testing; the primary-pair diagnostics in
Table~\ref{tab:primary_diagnostic} are the main result used in the text.

\begin{table}[htbp]
\centering
\caption{Full unadjusted component--response Spearman matrix for
$I_c(t)$--persistence-residual association. Correlations are exploratory
descriptor diagnostics.}
\label{tab:descriptor_residual}
\scriptsize
\setlength{\tabcolsep}{3pt}
\begin{tabular}{llrrrrr}
\toprule
Case & Component & Adv. rate & Torque & Thrust & Penetr. & Shield press. \\
\midrule
BSLL h=1 & Cutterhead & 0.238 & -0.286 & 0.048 & -0.095 & 0.095 \\
BSLL h=1 & Front shield & 0.381 & -0.429 & 0.190 & 0.095 & -0.095 \\
BSLL h=1 & Middle shield & -0.048 & 0.262 & 0.452 & -0.167 & 0.143 \\
BSLL h=1 & Tail shield & -0.238 & 0.286 & -0.048 & 0.095 & -0.095 \\
BSLL h=3 & Cutterhead & -0.214 & -0.714 & -0.036 & -0.214 & -0.536 \\
BSLL h=3 & Front shield & 0.286 & -0.179 & 0.143 & 0.250 & -0.750 \\
BSLL h=3 & Middle shield & 0.214 & 0.714 & 0.036 & 0.214 & 0.536 \\
BSLL h=3 & Tail shield & 0.179 & 0.750 & 0.107 & 0.143 & 0.571 \\
SJLS h=3 & Cutterhead & 0.860 & 0.446 & -0.669 & 0.669 & -0.912 \\
SJLS h=3 & Front shield & 0.640 & 0.152 & -0.350 & 0.475 & -0.745 \\
SJLS h=3 & Middle shield & -0.140 & -0.431 & 0.397 & -0.223 & -0.108 \\
SJLS h=3 & Tail shield & -0.843 & -0.314 & 0.740 & -0.804 & 0.775 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.95\linewidth]{fig8_descriptor_matrix_heatmap.pdf}
\caption{Full component--response Spearman correlation matrix between
$I_c(t)$ and persistence residuals. Values are exploratory and unadjusted for
multiple testing; they are used to inspect case-specific co-variation rather
than confirm universal component--response rules.}
\label{fig:descriptor_matrix_heatmap}
\end{figure}

\subsection{Descriptor-level null comparisons}
\label{sec:null_results}
The null-model comparison tests whether the primary associations can be
explained by simpler summaries. Figure~\ref{fig:null_model_comparison} shows
raw and detrended correlations for the proposed descriptor and five null
variants. The most important diagnostic is the detrended comparison, because
chainage and global anomaly can both carry strong monotonic trends in short
test intervals.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.95\linewidth]{fig9_null_model_comparison.pdf}
\caption{Descriptor-level null-model comparison for the fixed primary pairs.
Raw and linearly detrended Spearman correlations are shown for the proposed
descriptor and five simpler variants. The comparison evaluates descriptor
diagnostic structure, not prediction accuracy.}
\label{fig:null_model_comparison}
\end{figure}

\begin{table}[htbp]
\centering
\caption{Detrended Spearman correlations for the proposed descriptor and key
null variants.}
\label{tab:null_comparison}
\scriptsize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lrrrrr}
\toprule
Case & Proposed & Chainage & Global Vp & Distance only & Component shuffle \\
\midrule
BSLL h=1 & 0.214 & 0.000 & -0.190 & 0.000 & 0.167 \\
BSLL h=3 & -0.464 & -0.145 & -0.321 & 0.000 & 0.107 \\
SJLS h=3 & -0.772 & -0.009 & -0.179 & 0.000 & -0.135 \\
\bottomrule
\end{tabular}
\end{table}

For SJLS, the raw chainage-only and global-Vp associations are high, but both
collapse after detrending. The proposed descriptor retains a strong detrended
association, indicating that the component-specific geometry-weighted relation
representation contains more than a simple chainage trend. The BSLL null
comparisons are weaker and are read as limited-sample diagnostics.

\subsection{Sensitivity to graph thresholds and anomaly definitions}
\label{sec:sensitivity}
Figure~\ref{fig:descriptor_sensitivity} tests whether the fixed
$I_c(t)$--residual pairs are preserved when the edge-distance threshold
$\tau_{\mathrm{edge}}$ and normal-compatibility threshold $\eta_{\min}$ are
varied. The same descriptor--response pair is held fixed in each case, so the
figure evaluates threshold stability rather than searching for a new strongest
pair.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.95\linewidth]{fig7_descriptor_sensitivity.pdf}
\caption{Sensitivity of fixed $I_c(t)$--residual Spearman pairs to
graph-construction thresholds. Each panel varies $\tau_{\mathrm{edge}}$ and
$\eta_{\min}$ for one formal case while holding the descriptor--response pair
fixed.}
\label{fig:descriptor_sensitivity}
\end{figure}

Table~\ref{tab:anomaly_sensitivity} reports the anomaly-definition sensitivity
for the same fixed primary pairs. SJLS remains strong under Vp, Vs, and
mean(Vp, Vs) low-anomaly definitions after detrending. BSLL is more sensitive
to the anomaly definition, which is consistent with its compact test intervals.

\begin{table}[htbp]
\centering
\caption{Detrended Spearman correlations under alternative anomaly-score
definitions for the fixed primary pairs.}
\label{tab:anomaly_sensitivity}
\scriptsize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lrrrr}
\toprule
Case & Vp main & Vs low & Vp/Vs low & Mean(Vp, Vs) \\
\midrule
BSLL h=1 & 0.214 & -0.214 & 0.214 & -0.024 \\
BSLL h=3 & -0.464 & 0.179 & -0.179 & -0.643 \\
SJLS h=3 & -0.772 & -0.873 & 0.306 & -0.875 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Edge-level traceability}
\label{sec:traceability}
The graph representation also supports edge-level inspection. For the SJLS
primary pair, Figure~\ref{fig:traceability_example} shows the top contributing
candidate rock--machine relations at the selected chainage. Each contribution
is traceable to a rock voxel, a TBM surface node, a distance, a normal
compatibility value, a geometry weight, and a low-velocity anomaly score.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.95\linewidth]{fig10_traceability_example.pdf}
\caption{Traceability example for a primary diagnostic pair. Candidate
rock--machine edges are ranked by $w_{ij}^{rm}(t)q_i$, showing how the
component--chainage descriptor can be traced back to individual rock voxels and
TBM surface nodes.}
\label{fig:traceability_example}
\end{figure}

This traceability is the main practical difference between the proposed graph
descriptor and a monitoring-only residual curve. The descriptor can be queried
back to the spatial relation set that produced it, while the residual curve
alone cannot identify which geological voxels and machine components were
involved in the diagnostic summary.
